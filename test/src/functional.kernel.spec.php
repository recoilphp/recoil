<?php

declare (strict_types = 1); // @codeCoverageIgnore

namespace Recoil;

context('kernel', function () {

    xdescribe('->execute()', function () {
        //rit
        it('x', function () {});
    });

    xdescribe('->wait()', function () {
        it('x', function () {});
    });

    xdescribe('->interrupt()', function () {
        it('x', function () {});
    });
});
